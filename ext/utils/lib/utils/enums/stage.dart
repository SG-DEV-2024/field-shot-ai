enum Stage {
  exit,
  retry,
  next,
  // pause,
  // resume,
  ;

  // bool get isNone => this == none;
  // bool get isSuccess => this == success;
  // bool get isEnd => this == success || this == failed;
}
